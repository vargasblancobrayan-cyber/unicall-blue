# Unicall Blue

Aplicacion local de operaciones para call center construida con Next.js, TypeScript y Tailwind CSS.

## Ejecutar

```bash
npm install
npm run dev
```

Credenciales de prueba:

- Operador: `operador@unicallblue.co` / `ops123`
- Staff: `staff@unicallblue.co` / `staff123`
